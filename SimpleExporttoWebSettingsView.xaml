<UserControl x:Class="SimpleExporttoWeb.SimpleExporttoWebSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="20">
        <TextBlock Text="Export Output Directory:"/>
        <DockPanel Margin="0,5,0,15">
            <TextBox Text="{Binding Settings.OutputDirectory}" VerticalAlignment="Center"/>
        </DockPanel>

        <TextBlock Text="Webpage Title:"/>
        <TextBox Text="{Binding Settings.PageTitle}" Margin="0,5,0,0"/>
    </StackPanel>
</UserControl>